# Git SSH URL for the aletheia-data repo (cloned on first start)
ALETHEIA_DATA_REPO=git@github.com:youruser/aletheia-data.git

# Path to the SSH deploy key (used as Docker secret)
DEPLOY_KEY_PATH=~/.ssh/aletheia_deploy
